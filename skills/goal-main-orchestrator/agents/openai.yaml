interface:
  display_name: "Goal Main Orchestrator"
  short_description: "Run audited /goal branch waves."
  default_prompt: "Use $goal-main-orchestrator to run a prepared /goal bundle: bootstrap skills, audit prompts, create branch worktrees, and dispatch branch orchestrators."
