interface:
  display_name: "Goal Main Orchestrator"
  short_description: "Run audited rolling /goal branch pools with auditable Lite summaries."
  default_prompt: "Use $goal-main-orchestrator to run a prepared /goal bundle: bootstrap skills, audit prompts, optionally use deterministic CLI-only Lite summaries after audit or completed branch artifacts, create branch worktrees, and dispatch a rolling saturated pool of branch orchestrators within max_active_branch_agents."
