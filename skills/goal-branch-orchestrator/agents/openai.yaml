interface:
  display_name: "Goal Branch Orchestrator"
  short_description: "Dispatch branch workers with auditable Lite context routing."
  default_prompt: "Use $goal-branch-orchestrator to execute one audited branch prompt with optional deterministic CLI-only Lite advisory packets, allowed worker-route selection from the Gemini Pro -> Gemini Flash -> Codex Spark -> GitHub Copilot gpt-5.4 -> Codex mini ladder, and read-only reviewers."
