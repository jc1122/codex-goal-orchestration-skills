interface:
  display_name: "Goal Branch Orchestrator"
  short_description: "Dispatch Spark workers and branch reviewers."
  default_prompt: "Use $goal-branch-orchestrator to execute one audited branch prompt with CLI workers and reviewers."
