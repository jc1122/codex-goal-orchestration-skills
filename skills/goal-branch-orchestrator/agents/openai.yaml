interface:
  display_name: "Goal Branch Orchestrator"
  short_description: "Dispatch branch workers with optional Lite context routing."
  default_prompt: "Use $goal-branch-orchestrator to execute one audited branch prompt with optional CLI-only Lite advisory packets, Gemini Pro/Flash-first workers, GitHub Copilot gpt-5.4 high-effort fallback, Codex fallbacks, and read-only reviewers."
