interface:
  display_name: "Goal Preflight"
  short_description: "Prepare /goal orchestration bundles."
  default_prompt: "Use $goal-preflight to turn a roadmap or diagnosis into a linted /goal bundle and bootloader."
