interface:
  display_name: "Goal Preflight"
  short_description: "Build linted /goal bundles with telemetry requirements."
  default_prompt: "Use $goal-preflight to turn source material into a linted /goal bundle with branch prompts, packet telemetry requirements, optional deterministic CLI-only Lite advisory packets, and a location-bound bootloader."
