interface:
  display_name: "Goal Preflight"
  short_description: "Build linted, path-safe /goal bundles."
  default_prompt: "Use $goal-preflight to turn source material into a linted /goal bundle with branch prompts and a location-bound bootloader."
