[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
  "maintenance/reports",
  "node_modules",
]

[tool.ruff.lint]
select = ["F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["maintenance/reports"]
